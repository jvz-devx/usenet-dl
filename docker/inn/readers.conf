# INN readers.conf for usenet-dl testing
# Allow all access without authentication

# Default access for all connections
auth "default" {
    hosts: "*"
    default: "<usenet-dl-test>"
}

access "default" {
    users: "<usenet-dl-test>"
    newsgroups: "*"
    access: RPA
    post: *
}
